{
  "name": "prompt-maker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "sql.js": "^1.8.0",
    "zod": "^3.22.4",
    "axios": "^1.6.2"
  }
}